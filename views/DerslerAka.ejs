<!DOCTYPE html>
<html lang="tr">

<head>
    <meta charset="UTF-8">
    <title>Derslerim</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 20px;
        }

        .container {
            max-width: 1000px;
            margin: 0 auto;
            background: white;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        h1 {
            border-bottom: 2px solid #ddd;
            padding-bottom: 10px;
            color: #333;
        }

        .course-card {
            background: #fff;
            border: 1px solid #ddd;
            border-left: 5px solid #800000;
            margin-bottom: 15px;
            padding: 15px;
            border-radius: 4px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .course-info h3 {
            margin: 0 0 5px;
            color: #2c3e50;
        }

        .course-info p {
            margin: 0;
            color: #7f8c8d;
            font-size: 0.9em;
        }

        .back-btn {
            display: inline-block;
            margin-bottom: 20px;
            text-decoration: none;
            color: #fff;
            background-color: #7f8c8d;
            padding: 10px 15px;
            border-radius: 5px;
        }

        .back-btn:hover {
            background-color: #6c7a89;
        }
    </style>
</head>

<body>

    <div class="container">
        <a href="/dashboardAkd" class="back-btn">← Panele Dön</a>

        <h1>Verilen Dersler - <%= adSoyad %>
        </h1>

        <% if (dersler.length===0) { %>
            <p>Henüz atanmış dersiniz bulunmuyor.</p>
            <% } else { %>
                <% dersler.forEach(d=> { %>
                    <div class="course-card">
                        <div class="course-info">
                            <h3>
                                <%= d.ders_kodu %> - <%= d.ad %>
                            </h3>
                            <p>
                                <%= d.yariyil %>. Yarıyıl | Kredi: <%= d.kredi %> | AKTS: <%= d.akts %>
                            </p>
                            <p><i>
                                    <%= d.ogretim_sekli %> | <%= d.zorunlu_sec %>
                                </i></p>
                        </div>

                    </div>
                    <% }) %>
                        <% } %>

    </div>

</body>

</html>