<!DOCTYPE html>
<html lang="tr">

<head>
    <meta charset="UTF-8">
    <title>Not Girişi - Ders Seçimi</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 20px;
        }

        .container {
            max-width: 1000px;
            margin: 0 auto;
            background: white;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        h1 {
            border-bottom: 2px solid #ddd;
            padding-bottom: 10px;
            color: #333;
        }

        .back-btn {
            display: inline-block;
            margin-bottom: 20px;
            text-decoration: none;
            color: #fff;
            background-color: #7f8c8d;
            padding: 10px 15px;
            border-radius: 5px;
        }

        .back-btn:hover {
            background-color: #6c7a89;
        }

        .course-list {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 20px;
        }

        .course-card {
            background: #fff;
            border: 1px solid #ddd;
            border-top: 5px solid #800000;
            padding: 20px;
            border-radius: 4px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.05);
            transition: transform 0.2s;
        }

        .course-card:hover {
            transform: translateY(-5px);
        }

        .course-card h3 {
            margin-top: 0;
            color: #2c3e50;
        }

        .course-card p {
            color: #7f8c8d;
        }

        .action-btn {
            display: inline-block;
            margin-top: 10px;
            text-decoration: none;
            color: #fff;
            background-color: #800000;
            padding: 8px 20px;
            border-radius: 4px;
        }

        .action-btn:hover {
            background-color: #800000;
        }
    </style>
</head>

<body>

    <div class="container">
        <a href="/dashboardAkd" class="back-btn">← Panele Dön</a>

        <h1>Not Girişi Yapılacak Dersi Seçin</h1>

        <% if (dersler.length===0) { %>
            <p>Henüz atanmış dersiniz bulunmuyor.</p>
            <% } else { %>
                <div class="course-list">
                    <% dersler.forEach(d=> { %>
                        <div class="course-card">
                            <h3>
                                <%= d.ders_kodu %>
                            </h3>
                            <h4>
                                <%= d.ad %>
                            </h4>
                            <p>
                                <%= d.yariyil %>. Yarıyıl
                            </p>
                            <a href="/dashboardAkd/not-girisi/<%= d.id %>" class="action-btn">Not Gir →</a>
                        </div>
                        <% }) %>
                </div>
                <% } %>

    </div>

</body>

</html>